{
  "name": "npm-relative-version",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/azu/npm-relative-version.git"
  },
  "email": "azuciao@gmail.com",
  "homepage": "https://github.com/azu/npm-relative-version",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/azu/npm-relative-version/issues"
  },
  "version": "1.0.1",
  "description": "get relative version of the modules from npm.",
  "main": "lib/npm-relative-version.js",
  "files": [
    "lib",
    "src"
  ],
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run build",
    "test": "mocha"
  },
  "keywords": [
    "npm"
  ],
  "author": "azu",
  "dependencies": {
    "npm": "^3.5.3",
    "semver": "^5.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-preset-es2015": "^6.3.13",
    "espower-babel": "^4.0.1",
    "mocha": "^2.3.2",
    "npm-registry-mock": "^1.0.1",
    "power-assert": "^1.0.0"
  }
}
